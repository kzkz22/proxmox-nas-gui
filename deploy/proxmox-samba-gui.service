[Unit]
Description=Proxmox Samba GUI (Unraid-style SMB share manager)
After=network.target smbd.service
Wants=smbd.service

[Service]
Type=exec
User=root
WorkingDirectory=/opt/proxmox-samba-gui/backend
ExecStart=/opt/proxmox-samba-gui/venv/bin/uvicorn app.main:app \
    --host 0.0.0.0 --port 8481 \
    --ssl-certfile /etc/proxmox-samba-gui/cert.pem \
    --ssl-keyfile /etc/proxmox-samba-gui/key.pem
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
